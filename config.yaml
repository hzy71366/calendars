# ============================================================
# Calendar Engine — 用户配置
# ============================================================

config_version: 1

# --- 全局设置 ---
timezone: "Asia/Shanghai"
language: "zh-CN"

# --- 分类标签 ---
categories:
  - "佛教"
  - "斋日"

# --- 日历类型配置 ---
calendars:
  liuzhai:
    name: "六斋日"
    description: ""
    enabled: true
    emoji: "📿"
    alarm:
      enabled: true
      days_before: 0
      time: "06:00"
  shizhai:
    name: "十斋日"
    description: ""
    enabled: true
    emoji: "🪷"
    alarm:
      enabled: true
      days_before: 0
      time: "06:00"

# --- 年份范围 ---
years:
  ahead: 10
